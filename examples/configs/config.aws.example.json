{
  "_comment": "AWS IoT Core Configuration (mTLS authentication)",
  "_description": "Uses X.509 certificates for mutual TLS authentication with AWS IoT Core",

  "_comment_gateway_mac": "Gateway MAC address (12 hex digits, with or without colons). If omitted, automatically uses this device's MAC address",

  "mqtt": {
    "endpoint": "a1b2c3d4e5f6g7-ats.iot.us-east-1.amazonaws.com",
    "port": 8883,
    "client_id": "ble-gateway-001",
    "topic": "ble/gateway/data",
    "auth_type": "mtls",
    "qos": 1,
    "keepalive": 60,
    
    "_comment_certs": "Your AWS IoT certificates - get from AWS IoT Console",
    "ca_certs": "certs/AmazonRootCA1.pem",
    "certfile": "certs/certificate.pem.crt",
    "keyfile": "certs/private.pem.key"
  },
  
  "publish_interval_sec": 0.0,
  "throttle_control": true,
  "max_buffer_size": 100,
  "bluetooth_adapter": "hci0",
  "mac_whitelist": [],
  "name_whitelist": [],
  "manufacturer_id_whitelist": [],
  "service_uuid_whitelist": []
}
